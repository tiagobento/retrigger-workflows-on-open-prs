name: CI

on:
  pull_request:
    branches: "**"

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true

    steps:
      - uses: actions/checkout@v2

      - name: Run echo
        id: echo
        run: |
          echo "Everything works."
        shell: bash
